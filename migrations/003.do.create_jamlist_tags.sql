CREATE TABLE tags(
    tag_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    tag_content TEXT,
    user_id INTEGER REFERENCES users(id) ON DELETE CASCADE NOT NULL,
    jam_id INTEGER REFERENCES jams(id) ON DELETE CASCADE NOT NULL
);